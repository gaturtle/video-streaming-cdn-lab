spring.application.name=origin

# Directory on local disk the Origin serves video bytes from and stores uploads into.
# Defaults to the repo-root media/ dir (backend runs from backend/, hence ../media).
origin.storage.path=${ORIGIN_STORAGE_PATH:../media}

spring.servlet.multipart.max-file-size=5GB
spring.servlet.multipart.max-request-size=5GB
